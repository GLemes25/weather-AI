Aja como um Desenvolvedor Front-end Sênior especialista em TypeScript e Vite.

Estou desenvolvendo um aplicativo de Previsão do Tempo (com integração de IA para sugerir roupas) e quero adicionar uma funcionalidade: **o background (imagem de fundo do `<body>`) deve mudar dinamicamente de acordo com a condição climática atual da cidade pesquisada.**

tenho o @src/weather.ts

Por favor, atualize o meu código seguindo as regras abaixo:

1. **Mapeamento de Imagens:** Crie um dicionário (objeto) chamado `backgroundMap` que relacione a condição principal ("Clear", "Clouds", "Rain", "Snow", "Thunderstorm") com URLs de imagens locais em @public/img/backgrounds . Tenho uma imagem padrao para o plano de fundo, qunado nao tem clima dfdfinido que é @public/img/backgrounds/default.png ` Garanta que que o plano de fudno sempre exista, por isso a aexcisencia o deafult, as iamgens deve esar todas em tela cheia e garantindo responsividade.
2. **Função de Atualização:** Crie uma função `updateBackground(weatherCondition: string)` que acesse o DOM e mude a propriedade `backgroundImage` do `body`.
3. **Integração:** Chame essa função de atualização de background dentro do fluxo de sucesso (pode ser na função `renderWeather` ou logo após ela no `fetchWeatherData`).
4. **CSS:** Forneça o trecho de CSS necessário para aplicar no `body` e no meu `.container` para que a imagem de fundo tenha a propriedade `cover`, uma transição suave (`transition`) ao mudar, e para que o `.container` tenha um fundo levemente translúcido (efeito glassmorphism com `backdrop-filter: blur`) garantindo a legibilidade dos textos e botões sobre a imagem.

Forneça os códigos atualizados (TypeScript e CSS) separados de forma limpa e com breves comentários explicando a integração.
